import React from 'react';
import { Text, TouchableOpacity } from 'react-native';

const Button = () => {
  return (
    <TouchableOpacity>
      <Text>Hello1</Text>
    </TouchableOpacity>
  );
};

export default Button;
